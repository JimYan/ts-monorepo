import { TimestateInterceptor } from './timestate.interceptor';

describe('TimestateInterceptor', () => {
  it('should be defined', () => {
    expect(new TimestateInterceptor()).toBeDefined();
  });
});
